% Replacement caption for the existing mechanism-level Figure 7.
\caption{Mechanism-level comparison of propagated planning fields in the
Python simulation.  Set~1 shows the low-density occluded-merge interaction,
and Set~2 shows the corresponding high-density mixed-traffic interaction;
columns denote the reported times $T_1$--$T_4$.  DREAM, the ADA-sourced
control, and the APF-sourced control use the same planning backbone and differ
in their field source.  The snapshots illustrate the spatial field geometry
and associated trajectory response in representative episodes; aggregate
physical outcomes are evaluated separately in Fig.~\ref{fig:carla-two-condition-profiles}.}
